package scjp.com.java.chapter1;

public class MainClass
{
    public static void main( String[] args )
    {
        VarArgs varArgs = new VarArgs();
        // varArgs.varArgsMethod1( 1, 5, 6 );   // calling this method is not legal but defining them is legal, but again, there is no point of defining ambiguous 
        // methods, if we can't use them.

    }
}
